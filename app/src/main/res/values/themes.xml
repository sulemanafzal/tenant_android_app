<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tenant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Tenant" parent="Base.Theme.Tenant" />


    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:textSize">@dimen/_12ssp</item>
        <item name="android:drawable">@drawable/ic_back_left</item>
        <!-- Add more style customizations as needed -->
    </style>
</resources>